---
jobs:
  - job: Build_And_Test
    displayName: Run TOX
    steps:
      - task: Bash@3
        name: bump
        inputs:
          targetType: 'inline'
          workingDirectory: $(Build.Repository.LocalPath)
          script:
            #            git config --global user.email "david.ellis@iongroup.com" &&
            #git config --global user.name "davidmellis" &&
            #git config --global user.password $(secrets.GH_TOKEN) &&
            #git config --list --show-origin &&
            #pip install bumpversion && 
            bumpversion --allow-dirty --commit --tag --config-file=$(Build.Repository.LocalPath)/.bumpversion.toml  --list patch
#            git push origin HEAD:main
      - task: InstallSSHKey@0
        displayName: 'Install an SSH key'
        inputs:
          knownHostsEntry: 'SHA256:1|N0PFxiLeuuySLcYe7P4Oz9R4KcA=|RIxqIQ+xQ5xKBUi/9Dw2Tx2w2NE=  david.ellis@iongroup.com'
          sshPublicKey: 'id_rsa.pub'
          sshKeySecureFile: 'id_rsa'
          sshPassphrase: 'yieks7dry'
      - task: ShellScript@2
        name: push_new_version
        inputs:
          scriptPath: 'push.sh'
      - task: Bash@3
        name: dependencies
        inputs:
          targetType: 'inline'
          script: pip install tox pytest-azurepipelines
      - task: Bash@3
        name: tox
        inputs:
          targetType: 'inline'
          workingDirectory: $(Build.Repository.LocalPath)
          script: tox

